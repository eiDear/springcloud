server:
  #配置config服务器的服务端口，一般配置为8888
  port: 8888
spring:
  application:
    name: config-server
  #git服务配置信息
  cloud:
    config:
      server:
        git:
          uri: https://github.com/eiDear/config-repo

